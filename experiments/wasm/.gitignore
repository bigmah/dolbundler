# Build outputs
build/
*.o
*.wasm
*.dol
*.bin
bench/web/bench.js
spike/web/gxspike.js
bench_native
bench_wasm.js
swap_native
swap.js

# Anything the harnesses report back
reports.jsonl
frame.png

# A whole-game module is game-derived: never in this repo.
bench/web/full.wasm

# spike/frame.png is the one committed image; keep it despite frame.png above.
!spike/frame.png

# The browser client's module is built from the user's own disc.
web/strikers.js
web/strikers.wasm
# One module per game, and every one of them is derived from the user's disc.
web/gexe52.js
web/gexe52.wasm
frame-*.png
web/selftest.js
web/selftest.wasm
web/build-gxr/

# Frames read back from a run are pictures of the user's own game.
frame-*.png
ios/build/
.devcert/

# Cached gzip of the build outputs, made on demand by serve.py.
.gzcache/

# The lobby queue and its heartbeat (serve.py /next, queue-phone.sh).
next-url.txt
lobby-seen.txt
